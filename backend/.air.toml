root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./server/main.go"
  bin = "tmp/main"
  delay = 1000
  exclude_dir = ["tmp","vendor"]
  include_ext = ["go", "html"]

[log]
  color = true